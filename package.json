{
  "name": "hot-cups",
  "version": "0.1.0",
  "description": "Static site for Hot Cups (soups & juices) - Eleventy scaffold",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --watch",
    "build": "npx @11ty/eleventy",
    "start": "npx http-server docs -p 8080 -c-1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
